-t?o class Product v?i các tru?ng nhu yêu c?u
	+ t?o function add d? li?u c?a các tru?ng c?a Product
	+ function addQuantity() dùng d? tang thêm s? lu?ng c?a product
	+ function minusQuantity() dùng d? gi?m di s? lu?ng c?a Product
	+ function getQuantity() dùng d? tr? v? s? lu?ng hi?n t?i c?a product
	+ function getProduct() dùng d? xu?t t?t c? d? li?u c?a product
-t?o class Inventory v?i List<Product> products (danh sách s?n ph?m)
	+ function Inventory() dùng d? t?o m?i danh sách s?n ph?m
	+ function addProduct() dùng d? add thêm 1 product vào danh sách (list)
	+ function getInventory() dùng d? xu?t ra t?t c? thông tin c?a s?n ph?m (tr? id) và xem du?c s? lu?ng hàng t?n kho và t?ng giá tr? c?a chúng